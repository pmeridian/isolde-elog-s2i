#!/bin/bash -e
cat <<EOF
This is the isolde-elog-centos7 S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build elog isolde-elog-centos7 elog-app

You can then run the resulting image via:
docker run -p 8080 elog-app
EOF
